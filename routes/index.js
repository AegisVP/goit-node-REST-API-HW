const contactsRouter = require('./contactsRouter');
const userRouter = require('./userRouter');
const avatarRouter = require('./avatarRouter');

module.exports = { contactsRouter, userRouter, avatarRouter };
